# Iris v4.172 后台主动联系 + 文件发送

## 后台主动联系
Cloudflare 官方支持 Deploy to Cloudflare 按钮：
https://developers.cloudflare.com/workers/platform/deploy-buttons/

Worker 需要绑定 KV `IRIS_KV`、Secret `AI_API_KEY`，并设置每 5 分钟一次的 Cron。部署完成后把 `https://xxxx.workers.dev` 填进 Iris 的“主动联系后台地址”。

## 文件发送
v4.172 修复并增强了文件发送：
- 图片：继续走视觉模型；
- txt/md/json/csv/html/css/js/py/log/xml 等文本文件：文件内容会一起发给 AI；
- 其他文件：会作为文件消息保存，并显示“保存”按钮；当前不会把 PDF、Word、压缩包等二进制内容直接解析给 AI。
