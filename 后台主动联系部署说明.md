# Iris v4.171：后台主动联系

这次只新增“后台主动联系”能力，聊天、小窝、相册、宠物等原有数据仍然保存在本机。

## 你需要准备

1. 一个 Cloudflare 账号。
2. 一个 Workers KV Namespace。
3. 一个 Cloudflare Worker，把 `worker.js` 部署进去。
4. 给 Worker 配置 Secret：`AI_API_KEY`。
5. 给 Worker 绑定 KV，绑定名必须是 `IRIS_KV`。
6. Cron Trigger 设为每 5 分钟一次（`*/5 * * * *`）。

## 最简单的部署方式

### 1. 创建 KV

Cloudflare Dashboard → Workers & Pages → KV → Create namespace。

创建后复制它的 Namespace ID，填进 `wrangler.toml` 的 `id`。

### 2. 部署 Worker

把 `worker.js` 放到一个 Worker 项目中部署。

如果使用 Wrangler：

```bash
npx wrangler login
npx wrangler kv namespace create IRIS_KV
npx wrangler deploy
```

如果 Cloudflare 要求你先创建项目，也可以直接在 Workers 的代码编辑器里粘贴 `worker.js`，然后在设置里添加 KV 绑定。

### 3. 配置 AI Key

在 Worker 的 Settings → Variables and Secrets 里新增：

- `AI_API_KEY`：你的 DeepSeek API Key（选择 Secret）
- `AI_BASE_URL`：`https://api.deepseek.com`
- `AI_MODEL`：`deepseek-chat`

API Key 只放后台，不再由这个后台主动任务暴露给浏览器。

### 4. 配置 Cron

Worker → Triggers / Cron Triggers → 添加：

`*/5 * * * *`

意思是每 5 分钟检查一次有没有到主动联系时间。

### 5. 在 Iris 里填 Worker 地址

打开 Iris → 设置 → AI 设置 → 回复。

在“后台主动联系服务”里填你的 Worker 地址，例如：

`https://iris-proactive.你的子域.workers.dev`

然后保存。

## 工作方式

Iris 打开时会把最近 12 条聊天、精简记忆、共同记录和主动联系设置同步到后台。

Iris 关闭后，Cloudflare Cron 仍会运行。到了设定的主动联系间隔，Worker 调用 AI 生成一条消息并暂存在 KV。

下次打开 Iris 时，它会自动把待发送的消息收进当前聊天。

## 注意

- 这个版本实现的是“后台生成并保存消息”，不是强制弹出手机通知。
- 如果以后要做 iPhone 锁屏推送，再单独加 Web Push；这不会影响现在的聊天数据结构。
- 如果你继续用本机模式，不填“后台主动联系服务”，原来的前端主动联系逻辑仍然保留。
